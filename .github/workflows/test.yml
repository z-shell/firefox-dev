name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ghcr.io/z-shell/zd:latest
    steps:
      - run: zi pack for firfox-dev
